import styles from './FilterButton.module.scss';

const FilterButton = () => {
  return (
    <div className={styles.wrapper}>
      <h2>FilterButton</h2>
    </div>
  );
}

export default FilterButton;